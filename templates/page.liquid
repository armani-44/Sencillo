<div style="max-width:900px;margin:0 auto;padding:5rem 2rem;">
  <h1 style="font-weight:900;font-size:clamp(2rem,4vw,3rem);letter-spacing:-0.025em;text-transform:uppercase;color:var(--dark);margin-bottom:2rem;">{{ page.title }}</h1>
  <div style="font-size:0.95rem;line-height:1.8;color:#555;">{{ page.content }}</div>
</div>
